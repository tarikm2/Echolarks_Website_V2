$ep-gold 		: #EFC316;
$background-color	: #20201F;
$menu-item-color	: #FFFFFF;
$menu-item-alpha	: 0.66;

$footer-height		: 74px;
$body-border-gap	: 10px;

$echolarks-footer-size	: 450px;

$echolarks-e		: "Echolarks_E.png";
$echolarks-footer-logo	: "Echolarks_EP_Out_Now.png";


@import "compass/reset";
@import "compass/utilities";
@import "compass/css3";


html {
    height: 100%;
    background: $background-color;
}

body {
     position: absolute;
     top: 0; bottom: 0;
     left: 0; right: 0;
}



.border-corners {

    @include border-radius(25px);
    @include box-sizing(border-box);
    border: 1px $ep-gold;
    border-style: solid;
}

#body-border {

    width: 100%;
    height: 100%;
}

.body-container{
    
    @include box-sizing(border-box);	

    width: 100%; height: 100%;

    padding-left: $body-border-gap;
    padding-right: $body-border-gap;
    padding-top: $body-border-gap;
    padding-bottom: $footer-height;
}

#footer {
    position: fixed;
    width:100%;
    bottom: 0px;
}

#header {
    // position: fixed;
    width: 100%;
    // Left off here, setting the height of the header
    height: 140px;
}

.header-container {
    padding-top: 15px;
    padding-left: 22px;
    display: flex;
}

.header-logo {
    position: relative;
    width: 11%;
    z-index: 1;
}


#echolarks-header-E {
    @include background(image-url($echolarks_e));
    background-repeat: no-repeat;
    background-position: center left;
}


.header-menu {
    width: 100%;
    height: $footer-height;
}

.footer-container {
    display: flex;
    height: $footer-height;
}

.footer-menu-item {
    width: 33.33333%;
    margin-left: auto;
    margin-right: auto;
    width: (1/3) * 100%;
}

.footer-menu {
    position: relative;
    display: flex;
    width: (2/3) * 100%;
    z-index: -1;
}

.header-menu {
    position: relative;
    display: flex;
    width: (2/3) * 100%;
    padding-top: 44px;
}

.footer-item {
    align-items: center;
    justify-content: center;
}

.item-left {
    text-align: right;
    width: (1/3) * 40%;
}

.item-center {
    text-align: center;
    width: (1/3) * 150%;
}

.item-right {
    text-align: left;
}


#footer-text {
    color: $menu-item-color;
    opacity: $menu-item-alpha;
}

p {
    font-size: 12pt;
}

h1 {
    font-size: 25pt;
}

#header-text {
    color: $menu-item-color;
    opacity: $menu-item-alpha;
}


#echolarks-footer-logo {
    @include background(image-url($echolarks_footer_logo));
    background-repeat: no-repeat;
    background-size: $echolarks-footer-size;
    height: $footer-height;
    background-position: center left;
}


.footer-logo {
    position: relative;
    margin-left: 50px;
    width:(1/3) * 140%;
    z-index: 1;    
}





